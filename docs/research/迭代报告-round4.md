# 3D 工厂场景迭代报告 · Round 4（R4）

> 完成时间：R3 之后｜状态：✅ 完成并验证（回归测试自动化上线）

## R4 改动摘要

| 文件 | 改动参数 | 目的 |
|---|---|---|
| `src/scene/emissionInvariants.ts`（新增） | **发光不变量规则表**：15 项动态/静态发光体的强度范围，约束 ="全程 > Bloom 阈值 5.0 或 全程 < 5.0，仅平滑斜坡类可穿越"；`assertEmissionInvariants()` 违规即 console.error | 闪烁 bug 的**工程级回归防线**（修改发光参数必须同步规则表，表即约束文档） |
| `src/scene/PlantScene.ts` | 构造函数调用 `assertEmissionInvariants()` 并暴露 `this.invariants`（快照可读） | 与截帧工具链打通 |
| `src/connectors/pipes.ts` | 管线**流向箭头/脉冲胶囊**：MeshBasicMaterial → MeshStandardMaterial（emissiveIntensity 0.35/0.4，受光照着色） | 近景去"卡通纯色发光"感 |
| `src/env/environment.ts` | 地平线新增**太阳方位侧暖光带**（z=+262 可见侧，中心偏 -x 35m） | 天际线夕照指向性 |
| `src/layout/plantLayout.ts` | 反应区/塔区/球罐 3 个漫游机位微调（3/4 斜侧构图） | 漫游帧成片感 |
| `scripts/capture-frames.mjs` | ① 快照携带 invariants；② **运行时发光采样校验**（10×600ms 采样动态发光体 min/max vs 阈值，每轮截帧自动执行）；③ 新增 **--tourshots 近景多机位验收模式**（飞入 6 机位各 3 帧 + 时间方差分析） | 回归测试自动化 |

## R4 验证结果

### 回归测试（每轮截帧自动执行）
- **静态规则表：PASS**（快照 invariants 无违规）
- **运行时采样（8s）**：beacons 6.10~8.20、city 5.40~7.20、flame 7.56~10.17 —— **全部单侧于阈值 5.0**（光晕常驻，无穿越 = 无闪烁）✅

### 常规截帧（20 帧 × ~10s）
| 指标 | R3 | R4 | 结论 |
|---|---|---|---|
| 全局帧均值 | 101.04 | 101.0 | 稳定（脉冲材质替换几乎无亮度影响） |
| fps（软渲染） | 0.7 | 0.6 | 持平 |

### 近景多机位验收（--tourshots，3帧×~2.6s/机位）
| 机位 | mean | maxTemporalStd | hotPx(>20) |
|---|---|---|---|
| 全厂总览 | 96.5 | 27.1 | 11 |
| 罐区 | 61.8 | 32.3 | 5 |
| 反应区 | 79.3 | 41.4 | 25 |
| 塔区 | 79.6 | **51.2** | 22 |
| 产品球罐 | 77.5 | 35.5 | 11 |
| 配套区 | 84.6 | 22.8 | 1 |

近景时间方差主要来自：信标呼吸（6.1↔8.2）、蒸汽粒子上浮、火光/浮尘——均为**设计内的平滑动效**；3 帧离散采样放大了相位差。热点像素占比 ≤0.04%（25/57600），无"闪烁块"。

## R5 方案（收尾轮：场景密集度 + 文档交付）

| 优先级 | 内容 | 预期效果 |
|---|---|---|
| P1 | 管廊**电缆桥架**（上下层间细长槽线）+ 少量跨管廊桥 | 管廊密集度（大厂工艺建模的"密"） |
| P1 | 罐区围堰内**泡沫枪/灭火器箱**补充（喷淋环管已有） | 安全设施完整性 |
| P2 | 报告链收尾：验证数据汇总表（R0→R4 全指标）、修改文件清单、**git commit 建议** | 可回溯交付 |
| P2 | 用户真实浏览器人工复核指引（GPU 渲染观感 + 交互帧率） | 最终验收 |

验收：R5 截帧回归（invariants PASS + 热点不增）；构建通过；交付文档完整。