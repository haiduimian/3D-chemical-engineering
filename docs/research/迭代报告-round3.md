# 3D 工厂场景迭代报告 · Round 3（R3）

> 完成时间：R2 之后｜状态：✅ 完成并验证（无闪烁回归）

## R3 改动摘要

| 文件 | 改动参数 | 目的 |
|---|---|---|
| `src/env/sky.ts` | Preetham：**mieCoefficient 0.0038→0.0044**（太阳周边暖晕增强）、**rayleigh 1.0→0.95**（天顶略透蓝紫） | 太阳方位成为"发光锚点"，天空纵深层级更分明 |
| `src/env/environment.ts` | ① 云层改**双层**：夕照暖云（0x f0b27a, α0.16）×8 + 深灰暗云（0x544e4a, α0.13）×5；90s ② **rim 0.55→0.72、fill 0.22→0.26**（只提轮廓不明亮暗比）；③ 新增**次级核心阴影光**：与主太阳同方向（完全重合）、2048²、±50m 覆盖核心装置区（纹素密度 41px/m ≈ 主光 2.3×） | 逆光剪影边缘更亮 + 近景落影更密实 |
| `src/materials/pbr.ts` | ① 保温铝皮 clearcoatRoughness **0.35→0.28**；② 球罐 **iridescence 0.25**（抛光罐体薄膜虹彩） | 材质层次（大厂金属细节） |
| `src/materials/textures.ts` | 保温铝皮贴图 **256→512px** + **雨渍竖痕** + **锈蚀斑**（做旧）；法线图同步 512 | 塔身近景层次 |
| `src/scene/PlantScene.ts` | God Rays 采样 **56→48** | 性能平衡（效果不变） |

## R3 验证结果（像素级 20 帧 + 运行时快照）

| 指标 | R2 | R3 | 结论 |
|---|---|---|---|
| 全局帧均值 | 100.28 | **101.04**（+0.76） | 逆光强化按预期整体提亮（~0.75%） |
| 全局时间方差 | 0.008 | 0.015 | 仍极小（暗云层漂移所致），画面主体稳定 |
| 热点像素 max std | 35.7 | 36.8 | 无闪烁回归（灯/云缘呼吸量级不变） |
| 热点像素(std>30) | 10 px | 6 px | 位置仍集中在发光体/云缘区域 |
| SSAO/体积光 | — | 运行正常（passes 链完整） | — |
| 灯光快照 | — | 主光 3.4@4096²（±115）｜fill 0.26｜rim 0.72｜**core 0@2048²（±50）** | 三层光 + 次级阴影全部就位 |
| 性能 | fps~0.7（软渲染） | fps~0.7（软渲染，多一次 2048² 阴影 pass） | 软渲染下无恶化；真实 GPU 额外 ~1ms |

## R4 方案（下一轮：工程保障 + 细节收尾）

| 优先级 | 目标 | 内容 | 预期效果 | 涉及文件 |
|---|---|---|---|---|
| P0 | **闪烁回归测试自动化** | 把"动态发光强度 vs bloom 阈值 5.0"做成启动即校验的不变量（各发光体的 min/max 与阈值的上下界断言，违规 console.error + 报告）；纳入截图工具链快照 | 防止未来改动重新引入闪烁（"彻底修复"的工程保障） | PlantScene.ts、capture-frames.mjs 快照 |
| P0 | 近景质量 | 管线脉冲胶囊 MeshBasicMaterial → MeshStandardMaterial（emissive 低亮度，去除"卡通纯色发光"感）；箭头同材质化 | 对焦管线时画面不"玩具感" | pipes.ts |
| P1 | 地平线氛围 | horizonHaze 升级为**太阳方位侧暖光带**（第二层亮橙带，按 SUN_DIRECTION 方位放置）+ 微增主雾带 | 天际线亮度梯度更有"夕照"指向性 | environment.ts |
| P2 | 构图 | 漫游机位微调（塔区/球罐机位取"斜侧逆光"构图）+ 近景多机位截图验证流程（近景细节专项验收） | 漫游帧成片感；近景细节量化把关 | plantLayout.ts、capture 脚本 |
| P2 | 交付 | 全部迭代参数与验证数据汇总到 docs/research（round1~4 报告链）；建议 git commit 归档 | 可回溯、可复现 | docs/ |

验收：R4 截帧（管线近景热点数、地平线亮度梯度、fps）；回归测试零违规；真实浏览器人工复核（默认视角 + 2 个近景机位）。